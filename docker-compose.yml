version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data/checklists
    volumes:
      - ./data:/app/data
    restart: unless-stopped 